topBuffer
    fixed
contentHeight
    (max(baseHeight, baseHeight + delta) + floor) * scale
bottomBuffer
    fixed

scale : ratio of tallest (delta + floor) to fixed height container

canvasOrigin
    